- defaults:
    link:
        relink: true

- clean: ['~']

- link:
    ~/.bashrc: bashrc
    ~/.screenrc: screenrc
    ~/.gitconfig: gitconfig
    ~/.vim: vim

- shell:
    - [git submodule update --init --recursive, Installing submodules]
